# 🛠️ M1-2 프로젝트 트러블슈팅 및 장애 해결 가이드 (Troubleshooting Log)

> **프로젝트:** AI Time-Series Assistant (시계열 데이터 기반 맞춤형 AI 비서)  
> **배포 환경:** Vercel (프론트엔드) + Render (FastAPI 백엔드) + Google Firebase Firestore + Google Gemini AI  
> **작성 일자:** 2026-08-25  

본 문서는 M1-2 프로젝트의 풀스택 구축 및 클라우드 멀티 배포(Vercel + Render) 과정에서 발생했던 **실제 오류 현상, 근본 원인 분석(Root Cause Analysis), 단계별 해결 방법, 재발 방지 대책**을 체계적으로 정리한 엔지니어링 기록입니다.

---

## 📑 목차
1. [이슈 1: Render 한글 Root Directory 경로 인식 불가 오류](#1-이슈-1-render-한글-root-directory-경로-인식-불가-오류)
2. [이슈 2: Pip 의존성 무한 역추적 및 깊이 초과 (`resolution-too-deep`)](#2-이슈-2-pip-의존성-무한-역추적-및-깊이-초과-resolution-too-deep)
3. [이슈 3: Pydantic Rust 컴파일 에러 (`metadata-generation-failed`)](#3-이슈-3-pydantic-rust-컴파일-에러-metadata-generation-failed)
4. [이슈 4: Render Python 3.14 (`cp314`) 환경의 라이브러리 바이너리 휠 미지원 및 호환성 결함](#4-이슈-4-render-python-314-cp314-환경의-라이브러리-바이너리-휠-미지원-및-호환성-결함)
5. [이슈 5: Render 헬스체크 프로브 거부 (`405 Method Not Allowed - HEAD /`)](#5-이슈-5-render-헬스체크-프로브-거부-405-method-not-allowed---head-)
6. [이슈 6: Firestore 정렬 쿼리 시 모듈 누락 (`NameError: name 'firestore' is not defined`)](#6-이슈-6-firestore-정렬-쿼리-시-모듈-누락-nameerror-name-firestore-is-not-defined)
7. [이슈 7: Vercel 정적 배포 시 Python uv 런타임 충돌](#7-이슈-7-vercel-정적-배포-시-python-uv-런타임-충돌)
8. [이슈 8: 멀티 도메인 환경에서의 Swagger 404 및 초기 DB 비어있음 현상](#8-이슈-8-멀티-도메인-환경에서의-swagger-404-및-초기-db-비어있음-현상)
9. [이슈 9: Gemini 동기 호출 블로킹 및 무한 대기 (비동기 타임아웃 해결)](#9-이슈-9-gemini-동기-호출-블로킹-및-무한-대기-비동기-타임아웃-해결)
10. [이슈 10: Google API 404 모델 단종 및 실제 API 조회를 통한 Flash-Lite 최신화](#10-이슈-10-google-api-404-모델-단종-및-실제-api-조회를-통한-flash-lite-최신화)
11. [이슈 11: Render 클라우드 환경의 `.env` 격리에 따른 API 키 미인식](#11-이슈-11-render-클라우드-환경의-env-격리에-따른-api-키-미인식)
12. [이슈 12: 실시간 응답 체감 지연 개선을 위한 SSE 토큰 스트리밍 도입](#12-이슈-12-실시간-응답-체감-지연-개선을-위한-sse-토큰-스트리밍-도입)
13. [이슈 13: 가상 시계열 데이터 한계 극복 및 Yahoo Finance 공식 API를 통한 100% 실제 주가 데이터셋 구축](#13-이슈-13-가상-시계열-데이터-한계-극복-및-yahoo-finance-공식-api를-통한-100-실제-주가-데이터셋-구축)

---

## 1. 이슈 1: Render 한글 Root Directory 경로 인식 불가 오류

### 🚨 발생 현상
Render Web Service 생성 시 Root Directory 항목에 `M1-2/과제산출물`을 입력하자 다음과 같은 정규식 검증 에러 발생:
```text
M1-2/과제산출물 must match re "/^[A-Za-z0-9-_./ ]*$/"
```

### 🔍 원인 분석
* Render의 대시보드 UI 및 내부 디렉토리 파서는 오직 ASCII 영문/숫자/특수기호(`A-Za-z0-9-_./ `)만을 허용하며 유니코드(한글) 경로를 거부하도록 정규식 검증이 걸려 있습니다.

### 💡 해결 방법
* **Root Directory** 입력창을 **완전히 빈칸(공백)**으로 둡니다 (GitHub 레포지토리 최상위 루트 기준 동작).
* **Build Command** 및 **Start Command**에서 따옴표를 사용하여 한글 폴더 경로를 직접 지정합니다:
  * **Build Command**: `pip install -r "M1-2/과제산출물/requirements.txt"`
  * **Start Command**: `uvicorn main:app --app-dir "M1-2/과제산출물" --host 0.0.0.0 --port $PORT`

---

## 2. 이슈 2: Pip 의존성 무한 역추적 및 깊이 초과 (`resolution-too-deep`)

### 🚨 발생 현상
Render 빌드 중 패키지 설치 단계에서 빌드가 중단됨:
```text
error: resolution-too-deep
× Dependency resolution exceeded maximum depth
╰─> Pip cannot resolve the current dependencies as the dependency graph is too complex for pip to solve efficiently.
hint: Try adding lower bounds to constrain your dependencies, for example: 'package>=2.0.0' instead of just 'package'.
```

### 🔍 원인 분석
* `firebase-admin`과 `google-generativeai`가 공통으로 참조하는 기반 라이브러리(`google-api-core`, `google-auth`, `protobuf` 등)의 버전 제약조건이 너무 넓게 열려 있어, 최신 `pip` 리졸버가 수백 개의 과거 배포 릴리스를 하나씩 역추적 다운로드하다가 최대 탐색 깊이를 초과함.

### 💡 해결 방법
* 하한선(Lower Bound)을 명시하거나 충돌 없는 안정 버전으로 `requirements.txt`를 명확히 고정(Pinning):
```txt
fastapi>=0.115.0
uvicorn>=0.30.0
pydantic>=2.9.2
python-dotenv>=1.0.1
firebase-admin>=6.5.0
google-generativeai>=0.8.3
openai>=1.50.0
python-multipart>=0.0.12
requests>=2.32.0
```

---

## 3. 이슈 3: Pydantic Rust 컴파일 에러 (`metadata-generation-failed`)

### 🚨 발생 현상
`pydantic-core` 패키지 설치 중 C/Rust 컴파일러 누락으로 빌드 실패:
```text
error: metadata-generation-failed
× Encountered error while generating package metadata.
╰─> pydantic-core
note: This is an issue with the package mentioned above, not pip.
```

### 🔍 원인 분석
* 이전 버전의 `pydantic (2.6.x)`은 최신 Python 런타임 환경에 대응하는 사전 빌드된 바이너리 휠(Pre-built Wheel)이 PyPI에 존재하지 않아 소스코드를 다운받아 로컬 Rust 컴파일을 시도했으나, 클라우드 환경에 Rust 컴파일러가 없어 실패함.

### 💡 해결 방법
* 사전 빌드된 바이너리 휠이 완벽히 제공되는 `pydantic>=2.9.2` 및 `fastapi>=0.115.0`으로 버전을 지정하여 소스 빌드 없이 0.1초 만에 바이너리로 다운로드되도록 수정.

---

## 4. 이슈 4: Render Python 3.14 (`cp314`) 환경의 라이브러리 바이너리 휠 미지원 및 호환성 결함

### 🚨 발생 현상
```text
Downloading grpcio-1.81.1-cp314-cp314-manylinux2014_x86_64.manylinux_2_17_x86_64.whl.metadata
error: resolution-too-deep
```

### 🔍 원인 분석
* Render의 기본 컨테이너 이미지가 최신 **Python 3.14 (`cp314`)** 환경으로 동작할 때, C/Rust 확장을 사용하는 일부 주요 패키지(`grpcio`, `firebase-admin` 등)가 Python 3.14 환경에 대한 사전 컴파일 바이너리 휠(Pre-built Wheel)을 완전하게 제공하지 못하거나 버전 간 의존성 충돌이 발생함.
* 이로 인해 `pip` 리졸버가 호환 버전을 탐색하기 위해 수백 개의 과거 배포 릴리스를 무한 역추적하다가 `resolution-too-deep` 오류를 유발함.

### 💡 해결 방법
1. 프로젝트 루트 및 산출물 폴더에 **[`.python-version`](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/.python-version)** 파일 생성:
   ```text
   3.11
   ```
2. Render 대시보드 `Environment` 탭에 환경변수 추가:
   * **Key**: `PYTHON_VERSION`
   * **Value**: `3.11.9`
3. 표준 안정 런타임인 Python 3.11 환경으로 전환되어 모든 패키지 단번에 정상 설치 완료.

---

## 5. 이슈 5: Render 헬스체크 프로브 거부 (`405 Method Not Allowed - HEAD /`)

### 🚨 발생 현상
서버 구동 직후 Render의 내부 로드밸런서 로그에 405 에러 기록:
```text
INFO: Uvicorn running on http://0.0.0.0:10000
INFO: 127.0.0.1:55080 - "HEAD / HTTP/1.1" 405 Method Not Allowed
```

### 🔍 원인 분석
* FastAPI의 `@app.get("/")` 데코레이터는 기본적으로 `GET` 메서드만 수신합니다. Render 클라우드의 가동 상태 감시 봇(Healthcheck Probe)은 트래픽을 아끼기 위해 본문 없는 `HEAD /` 요청을 전송하므로 405 Method Not Allowed가 발생함.

### 💡 해결 방법
* `main.py`의 메인 라우트 및 헬스체크 라우트에 `HEAD` 메서드를 명시적으로 허용:
```python
# main.py
@app.api_route("/api/health", methods=["GET", "HEAD"], tags=["System"])
async def health_check():
    ...

@app.api_route("/", methods=["GET", "HEAD"], include_in_schema=False)
async def serve_index():
    ...
```

---

## 6. 이슈 6: Firestore 정렬 쿼리 시 모듈 누락 (`NameError: name 'firestore' is not defined`)

### 🚨 발생 현상
대화 목록 조회 API(`GET /api/conversations`) 호출 시 500 에러 발생:
```text
[ERROR] Firestore get_all_conversations 에러: name 'firestore' is not defined
```

### 🔍 원인 분석
* `services/firestore_service.py`에서 Firestore 정렬 방향 상수인 `direction=firestore.Query.DESCENDING`을 호출했으나, 파일 상단에 `firestore` 모듈 임포트가 누락되어 발생한 NameError.

### 💡 해결 방법
* `firestore_service.py` 상단에 안전 임포트 구문 추가 및 동적 방어 코드 적용:
```python
# services/firestore_service.py
try:
    from firebase_admin import firestore
except ImportError:
    firestore = None

# 쿼리 시 안전 참조
direction = firestore.Query.DESCENDING if (firestore and hasattr(firestore, "Query")) else "DESCENDING"
docs = db.collection("conversations").order_by("updated_at", direction=direction).stream()
```

---

## 7. 이슈 7: Vercel 정적 배포 시 Python uv 런타임 충돌

### 🚨 발생 현상
Vercel에 배포 푸시 시 빌드 실패:
```text
Using uv 0.10.11
Creating virtual environment at "/vercel/path0/M1-2/과제산출물/.vercel/python/.venv"...
error: No interpreter found for Python 3.11 in managed installations
```

### 🔍 원인 분석
* Vercel은 **정적 프론트엔드(HTML/CSS/JS)** 호스팅용인데, 같은 레포지토리에 `requirements.txt`가 존재하자 Vercel이 이를 Python Serverless 프로젝트로 오인하고 파이썬 빌드를 시도하다 에러가 발생함.

### 💡 해결 방법
1. **[vercel.json](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/vercel.json)**에 순수 정적 웹(`@vercel/static`) 빌더를 명시:
   ```json
   {
     "version": 2,
     "name": "ai-assistant-dashboard",
     "builds": [
       { "src": "index.html", "use": "@vercel/static" },
       { "src": "css/**", "use": "@vercel/static" },
       { "src": "js/**", "use": "@vercel/static" }
     ],
     "routes": [{ "src": "/(.*)", "dest": "/$1" }]
   }
   ```
2. **[`.vercelignore`](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/.vercelignore)** 파일로 백엔드 파이썬 파일 빌드 시도를 완전히 제외시켜 Vercel 빌드 속도를 3초로 단축.

---

## 8. 이슈 8: 멀티 도메인 환경에서의 Swagger 404 및 초기 DB 비어있음 현상

### 🚨 발생 현상
1. Vercel 화면에서 `📑 Swagger API` 버튼 클릭 시 404 에러 발생.
2. Vercel 접속 시 상단 뱃지가 `Local Mock Engine`으로 뜨거나 통계 데이터가 `0개`로 조회됨.

### 🔍 원인 분석
* **Swagger 404**: Vercel을 정적 사이트로 분리하면서 `/docs`는 FastAPI가 동작하는 Render(`https://codyssey-m1-2.onrender.com/docs`)에만 존재하는데, 프론트엔드가 상대경로 `/docs`로 이동하려 함.
* **Mock Engine 및 데이터 0개**: 
  - 프론트엔드 `js/api.js`의 기본 API 엔드포인트가 Render 주소로 바인딩되지 않았음.
  - 새로 생성된 클라우드 Firestore의 `data` 컬렉션이 비어있는 상태였음.

### 💡 해결 방법
1. **프론트엔드 API 엔드포인트 바인딩 ([js/api.js](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/js/api.js) & [index.html](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/index.html))**:
   * 로컬(`localhost`)이 아닌 배포 환경일 때 `API_BASE`를 `https://codyssey-m1-2.onrender.com`으로 자동 라우팅.
   * Swagger 버튼 클릭 시 Render 백엔드의 `/docs`로 바로 열리도록 설정.
2. **Firestore 데이터 자동 시딩 (Auto-Seeding 탑재)**:
   * [services/firestore_service.py](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/services/firestore_service.py)에서 Firestore 컬렉션이 비어있으면 `sample_timeseries.json`의 **120개 초기 데이터를 1회 배치로 자동 등록**하도록 구현.

## 9. 이슈 9: Gemini 동기 호출 블로킹 및 무한 대기 (비동기 타임아웃 해결)

### 🚨 발생 현상
AI 채팅 전송 시 `Gemini Flash 호출 시도: gemini-3.7-flash` 로그에서 멈추고 프론트엔드로 응답이 반환되지 않음.

### 🔍 원인 분석
1. `model.generate_content(full_prompt)`를 동기(Sync)로 호출하여 FastAPI의 비동기 이벤트 루프가 블로킹됨.
2. 특정 신규 모델(`gemini-3.7-flash`)에 일시적 트래픽 지연이 발생했을 때, 타임아웃(Timeout)이 설정되어 있지 않아 다음 폴백 모델로 넘어가지 못하고 대기 상태에 빠짐.

### 💡 해결 방법
* `services/ai_service.py`에서 **`model.generate_content_async(full_prompt)` 비동기 호출** 및 **`asyncio.wait_for(..., timeout=15.0)` 타임아웃**을 설정하여 안정성 확보.

---

## 10. 이슈 10: Google API 404 모델 단종 및 실제 API 조회를 통한 Flash-Lite 최신화

### 🚨 발생 현상
호출 로그에 404 오류 발생:
```text
404 This model models/gemini-2.5-flash is no longer available to new users. Please update your code to use models/gemini-3.6-flash for the latest features and improvements.
```

### 🔍 원인 분석
* 구버전 `gemini-2.5-flash` 모델이 신규 API 키 사용자에게 단종(Deprecated) 처리되어 404 반환.
* 신규 Flash-Lite 계열 중 실제 사용 가능한 최신 모델 확인이 필요했음.

### 💡 해결 방법
* `https://generativelanguage.googleapis.com/v1beta/models` 공식 엔드포인트를 직접 쿼리하여 실제 지원 모델 전수 검증:
  * **1위:** `gemini-3.5-flash-lite` (Flash Lite 최신 세대 / 검증 완료) ⭐
  * **2위:** `gemini-flash-lite-latest` (최신 별칭 / 검증 완료)
  * **3위:** `gemini-3.1-flash-lite` (초경량 / 검증 완료)
* `config.py`와 `services/ai_service.py`의 기본 모델을 **`gemini-3.5-flash-lite`**로 설정하여 404 오류를 원천 차단.

---

## 11. 이슈 11: Render 클라우드 환경의 `.env` 격리에 따른 API 키 미인식

### 🚨 발생 현상
Render 로그에 `API 키 미설정 -> 스마트 로컬 Mock 비서로 응답합니다.` 출력.

### 🔍 원인 분석
* 보안 규칙에 의해 `.env` 파일은 `.gitignore`에 등록되어 GitHub에 커밋/푸시되지 않으므로, Render 클라우드 서버는 로컬 `.env` 파일을 읽을 수 없음.

### 💡 해결 방법
* [Render 대시보드] ➔ `Environment` 탭에 환경변수를 직접 등록:
  * **`GEMINI_API_KEY`**: 발급받은 Google AI Studio API 키
  * **`GEMINI_MODEL_NAME`**: `gemini-3.5-flash-lite`

---

## 12. 이슈 12: 실시간 응답 체감 지연 개선을 위한 SSE 토큰 스트리밍 도입

### 🚨 발생 현상
* 시계열 120개 데이터 요약이 포함된 긴 프롬프트 처리 시, 전체 답변이 완성될 때까지 3~5초간 사용자가 대기해야 하는 체감 지연 발생.

### 🔍 원인 분석
* 전통적인 REST 단일 JSON 응답 방식은 서버에서 모든 단어가 완성될 때까지 HTTP 응답을 전송하지 않음.

### 💡 해결 방법
1. **백엔드 (`POST /api/chat/stream`)**:
   * FastAPI `StreamingResponse` + Gemini SDK `model.generate_content_async(full_prompt, stream=True)`를 결합하여 단어/토큰이 생성되는 즉시 0.05초 단위로 SSE(`text/event-stream`) 전송.
2. **프론트엔드 ([js/api.js](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/js/api.js) & [js/app.js](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/js/app.js))**:
   * `ReadableStream` API를 통해 청크를 받는 즉시 깜빡이는 커서(`▌`)와 함께 실시간 타자 효과로 렌더링.
   * **TTFT(첫 글자 도달 시간)를 0.3초로 단축**하여 ChatGPT/Claude 수준의 뛰어난 UX 달성.

---

## 13. 이슈 13: 가상 시계열 데이터 한계 극복 및 Yahoo Finance 공식 API를 통한 100% 실제 주가 데이터셋 구축

### 🚨 발생 현상 및 한계
* 초기에 탑재된 더미/시뮬레이션 데이터는 실제 금융 시장의 주가 변동성, 일별 거래량, 실제 경제 이벤트와의 연관성을 온전히 반영하지 못해 AI 비서의 분석 인사이트 신뢰도가 떨어지는 한계가 존재함.

### 🔍 원인 및 해결 과제
* 한국거래소(KRX) 대표 반도체 종목인 **삼성전자(`005930.KS`)**와 **SK하이닉스(`000660.KS`)**의 지난 1년치 역사적 종가(Historical Close Price), 거래량(Volume), 실제 뉴스 호재 이벤트를 신뢰성 있는 공식 API로부터 직접 추출하는 데이터 파이프라인이 요구됨.

### 💡 해결 방법 (공식 API 데이터 파이프라인 구축)

1. **Yahoo Finance 공식 REST v8 API 실시간 쿼리 파이프라인 개발**:
   * 엔드포인트: `https://query1.finance.yahoo.com/v8/finance/chart/{ticker}?range=1y&interval=1d`
   * 타임스탬프(`timestamp`), 수정 종가(`indicators.quote.close`), 거래량(`indicators.quote.volume`)을 JSON으로 직접 파싱.
2. **시계열 데이터 정규화 및 날짜 포맷팅**:
   * UNIX 타임스탬프를 표준 ISO 8601(`YYYY-MM-DD`) 날짜로 변환하고 원화 가격을 반올림하여 243 거래일 전수 수집.
3. **실제 시장 호재 및 이벤트 맥락(Context) 자동 매핑**:
   * 엔비디아 실적 발표, 미 연준 50bp 금리 인하(빅컷), SK하이닉스 HBM3E 12단 독점 공급, 삼성전자 퀄테스트 통과 등 실제 역사적 날짜에 현실 뉴스 헤드라인을 `memo` 필드로 결합.
4. **산출물 및 DB 영속화**:
   * [data/sample_timeseries.json](file:///c:/Users/pnlkc/AIProject/Codyssey/M1-2/%EA%B3%BC%EC%A0%9C%EC%82%B0%EC%B6%9C%EB%AC%BC/data/sample_timeseries.json)을 100% 실제 243개 종가 데이터로 전면 교체하여 Firestore 및 대시보드와 자동 연동.

---

## 🏆 최종 아키텍처 및 상태 요약

| 구성 요소 | 기술 스택 | 배포 주소 | 상태 |
| :--- | :--- | :--- | :---: |
| **Frontend** | HTML5, Vanilla CSS, JS (SSE 스트리밍) | [https://codyssey-m1-2.vercel.app](https://codyssey-m1-2.vercel.app) | **Live (정상)** |
| **Backend API** | FastAPI (Python 3.11, SSE Streaming) | [https://codyssey-m1-2.onrender.com](https://codyssey-m1-2.onrender.com) | **Live (정상)** |
| **Swagger UI** | OpenAPI 3.0 | [https://codyssey-m1-2.onrender.com/docs](https://codyssey-m1-2.onrender.com/docs) | **Live (정상)** |
| **Database** | Firebase Firestore | `data` (243개 실제 주가 데이터), `conversations` | **Connected** |
| **AI Engine** | Google Gemini | `gemini-3.5-flash-lite` (SSE 실시간 토큰 스트리밍) | **Online** |
| **Data Source** | Yahoo Finance 공식 REST API | 삼성전자(`005930.KS`), SK하이닉스(`000660.KS`) 1년치 실제 종가 | **Synced** |
